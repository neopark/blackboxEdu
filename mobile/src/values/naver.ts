import Config from "react-native-config";

const naver = {
  consumerKey: Config.NAVER_CONSUMER_KEY,
  consumerSecret: Config.NAVER_CONSUMER_SECRET,
  urlScheme: Config.NAVER_URL_SCHEME,
};

export default naver;
